import { LandingPageView } from '@/components/home/LandingPageView'

/** Public landing — marketing layout from landing-page (1).html with live elections catalog. */
export function HomePage() {
  return <LandingPageView />
}
